databases:
  - name: verinews-db
    plan: free
    databaseName: verinews
    user: verinews

services:
  - type: web
    name: verinews
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn config.wsgi:application --workers 1 --threads 2"

    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.prod

      - key: DJANGO_SECRET_KEY
        generateValue: true

      - key: DATABASE_URL
        fromDatabase:
          name: verinews-db
          property: connectionString

      - key: PYTHON_VERSION
        value: "3.12.3"

      - key: RESEND_API_KEY
        sync: false

      - key: DEFAULT_FROM_EMAIL
        sync: false

    healthCheckPath: /healthz/