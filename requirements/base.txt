# Packages needed everywhere (dev AND production).
# Pinned versions = reproducible installs; nobody gets a surprise
# breaking change because a package auto-updated.
Django==5.0.14
djangorestframework==3.15.2
drf-spectacular==0.30.0
whitenoise==6.7.0